<h2>Editing Book</h2>

<%= form_with model: @book do |f| %>

  <div>
      <label>Titel</label>
      <%= f.text_filed :titel%>
  </div>
  <div>
      <label>Opinion</label>
      <%= f.text_area :opinion%>
  </div>
  <div>
      <%= f.submit "Update Book" %>
  </div>
 <% end %>

 <%= link_to "Show" book_path(book.id) %>
 "|"
 <%= link_to "Back" books_path %>